@model BlogPost

<h2>@Model.Title</h2>

<p>
    @Model.Content
</p>
<p>
    <small>
        Oluşturulma Tarihi: @Model.CreatedDateTime (@ViewData["CreatedAgo"])
    </small>
</p>

<a asp-action="index" class="btn btn-secondary">Geri Dön.</a>